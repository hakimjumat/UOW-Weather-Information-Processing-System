#ifndef CITY_H
#define CITY_H
#include <iostream>
#include <string>
#include <vector>
using namespace std;

void City(int height, int width, vector<string>& city_file, string**& city_map, vector<string> &city_name, vector<string> &city_id);
#endif
